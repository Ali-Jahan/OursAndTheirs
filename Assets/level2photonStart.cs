using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class level2photonStart : MonoBehaviourPunCallbacks
{
    
    // public GameObject red;
    //
    // public GameObject blue;
    // // Start is called before the first frame update
    // void Start()
    // {
    //     PhotonNetwork.Instantiate(red.name, new Vector3(9.3f, -3.4f, 0), Quaternion.identity);
    //     PhotonNetwork.Instantiate(blue.name, new Vector3(19f, -3.4f, 0), Quaternion.identity);
    // }

    // Update is called once per frame
    void Update()
    {
        
    }
}
